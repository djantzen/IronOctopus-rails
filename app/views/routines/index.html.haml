.container
  %h4 Routines
  %ul
    - @routines.each do |routine|
      %li
        = link_to(routine.name, user_routine_path(routine.client, routine))
        .btn-group
        = link_to("Edit", edit_user_routine_path(routine.client, routine), :class => "btn")
        = link_to("Perform", perform_routine_path(routine.client, routine), :class => "btn")
  = link_to("Create New Routine", new_user_routine_path(@client), :class => "btn btn-primary")
